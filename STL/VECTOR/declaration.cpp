#include <bits/stdc++.h>
using namespace std;

//AIM : 
int main(){
    int n;
    /*

                                // INITIALIZATION 

    vector <int> v {10 ,2 ,3};
    vector <int> v (n); //A vector of size n
    vector <int> v (n,val); //A vector of size n with value of each element = val

    // using array
    int arr={1,4 ,5 ,7 ,8};
    n=sizeof(arr)/sizeof(arr[0]);
    vector <int> v(arr,arr+n);
 
                                // TRAVERSAL 


    // METHOD 1
    for(int x:v){
        x=6; //This wont change the value of elements. to do so u need to pass address 
    }

    for(int &x:v){
        x=8; // It will work
    }
    for(int x:v){
        cout << x <<" ";
    }

    // METHOD 2 : USING ITERATOR
    vector <int> :: iterator i;
            // (a)
    for(i=v.begin(),i!=v.end(),i++){
        cout << *i << " ";        
    }
            // (b)
    for(auto j=v.begin(),j!=v.end(),j++){
        cout << *j << " ";        
    }
    */


    return 0;
}