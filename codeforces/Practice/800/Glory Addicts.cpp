#include <bits/stdc++.h>
using namespace std;
#define ll long long int
// AIM :
int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        int n,z=0;
        cin >> n;
        ll b[n];
        int a[n];
        for (int i = 0; i < n; i++) {cin >> a[i];
        if (a[i]==0) z++;}
        for (int i = 0; i < n; i++) cin >> b[i];
        
    }

    return 0;
}