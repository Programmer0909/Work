#include <bits/stdc++.h>
using namespace std;

// AIM :
int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        int x, y;
        cin >> x >> y;
        if (x==2 && y==3)
        {
            cout << 1 << " " << 2;
        }
        if (x==3 && y==2)
        {
            cout << 2 << " " << 1;
        }
        if (x==3 && y==3)
        {
            cout << 2 << " " << 2 << endl;
        }
        if (x>3 && )
        {
            /* code */
        }
        
        
        
    }

    return 0;
}